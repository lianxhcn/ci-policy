# AGENTS.md 模板

这是给教师或研究者自建讲义项目使用的简化模板。复制到自己的项目根目录后，再根据课程内容补充具体规则。项目中的正式 `AGENTS.md` 是本地 Agent 工作文件，不作为公开仓库文件提交。

## 项目定位

- 说明讲义服务的读者和研究主题。
- 明确哪些目录是正式内容，哪些目录是本地过程资料。
- 不把某一次课程的日期、地点或班级写入可复用讲义。

## 写作主线

围绕以下顺序组织政策评估材料：

```text
政策目标 → 政策实施机制 → 数据形成机制 → 识别威胁 → 研究设计 → 估计方法 → 结果解释
```

方法名称不应替代研究问题。每个公式、图表和代码示例都要说明它解决什么识别困难，以及依赖什么假设。

## 文件规则

- 正式章节使用 `.qmd`。
- 可运行代码放入 `notebooks/` 或 `scripts/`。
- 原始文献和本地过程资料放入 `refers/`、`working/`，并加入 `.gitignore`。
- 不在正式正文中写入本机绝对路径、账号信息、密钥或未公开材料。
- 重要公式、图和表使用项目名前缀，例如 `eq:project-did-main`、`fig:project-workflow`。

## 质量检查

提交前至少检查：

- Quarto 是否能够完整渲染；
- 章节和附录链接是否有效；
- 公式、图表交叉引用是否正常；
- 文献、DOI、PDF 和代码链接是否可以核查；
- 是否误提交本地路径或临时草稿。
