# 参与维护本讲义

本项目是一套可复用的 Quarto 在线讲义，主题是「因果推断：政策评估中的识别思维与实证设计」。欢迎教师和研究者基于本项目备课、提出修改建议或维护自己的课程版本。

## 内容结构

- `lectures/`：正式章节和阅读型附录。
- `notebooks/`：学生可以运行的 Notebook、Stata do-file 和示例数据。
- `resources/`：阅读地图、政策说明和辅助材料。
- `task-prompt/`：公开的备课规划和写作提示。
- `settings/`：面向学员的环境配置说明。
- `working/`、`refers/`：本地过程文件和原始资料，不提交到公开仓库。

## 写作原则

每个方法都应从具体的政策评估问题出发，按照以下顺序组织：

```text
政策目标 → 政策实施机制 → 数据形成机制 → 识别威胁 → 研究设计 → 估计方法 → 结果解释
```

正式章节使用 `.qmd`。不要在正文中写入本机绝对路径、未核查的 DOI、复现链接或未公开材料。公式、图和表使用带 `cipolicy-` 前缀的唯一交叉引用标签。

## 本地渲染

安装 Quarto 1.6 或更高版本后，在项目根目录运行：

```powershell
quarto render --no-execute
```

`--no-execute` 适合构建公开网页，因为部分 Notebook 依赖本地 Stata 或 `nbstata`。正式发布前，请在本地单独检查需要运行的 Notebook。

## 提交修改

修改章节时，请说明：

- 修改了哪一章或哪一个 Notebook；
- 使用了什么公开来源；
- 是否改变了公式、图表或交叉引用；
- 是否已经成功运行 Quarto 渲染。

请不要提交 `refers/`、`working/`、`AGENTS.md`、`CLAUDE.md`、`.claude/` 或包含本机路径的配置归档。
